apiVersion: v1
kind: Secret
metadata:
  name: queryforge-secret
  namespace: queryforge
  labels:
    app.kubernetes.io/name: queryforge
type: Opaque
stringData:
  # Copy this file to secret.yaml, replace every placeholder, then apply it:
  # kubectl apply -f deploy/k8s/secret.yaml
  OPENAI_API_KEY: "__REQUIRED__"
  DASHSCOPE_API_KEY: "__REQUIRED__"
  POSTGRES_PASSWORD: "__REQUIRED_STRONG_PASSWORD__"
  QUERY_POSTGRES_PASSWORD: "__REQUIRED_STRONG_PASSWORD__"
  REDIS_PASSWORD: ""
  JWT_SECRET: "__REQUIRED_AT_LEAST_32_BYTES_RANDOM_SECRET__"
  LANGFUSE_PUBLIC_KEY: ""
  LANGFUSE_SECRET_KEY: ""

