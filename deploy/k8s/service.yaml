apiVersion: v1
kind: Service
metadata:
  name: queryforge-api
  namespace: queryforge
  labels:
    app.kubernetes.io/name: queryforge
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: queryforge
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http

